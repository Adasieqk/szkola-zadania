1. SELECT plik, nr_klienta, liczba_odbitek FROM zamowienia WHERE rodzaj LIKE "matowy" AND liczba_odbitek > 100;
2. SELECT (liczba_odbitek * cena) AS 'Do zapłaty' FROM zamowienia NATURAL JOIN zdjecia WHERE zamowienia.nr_klienta = 3;
3. SELECT SUM(liczba_odbitek) FROM zamowienia WHERE rodzaj = "błyszczący";
4. CREATE USER "Anna"@"localhost" IDENTIFIED BY "@NNa";